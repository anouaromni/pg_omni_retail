# Reference this view as analytics_prod_ani_prod__supplies
catalog: ANALYTICS_PROD
schema: ANI_PROD
table_name: SUPPLIES

dimensions:
  supply_uuid:
    sql: '"SUPPLY_UUID"'

  supply_id:
    sql: '"SUPPLY_ID"'
    format: ID

  product_id:
    sql: '"PRODUCT_ID"'
    format: ID

  supply_name:
    sql: '"SUPPLY_NAME"'

  supply_cost:
    sql: '"SUPPLY_COST"'

  is_perishable_supply:
    sql: '"IS_PERISHABLE_SUPPLY"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: supplies
  target_schema: ANI_PROD
  config:
    materialized: table
  code: |-
    with

    supplies as (

        select * from {{ ref('stg_supplies') }}

    )

    select * from supplies
  referenced_by: [ supply_cost_summary ]
