# Reference this view as analytics_prod_dbt_ahnini__stg_order_items
#This description was pulled from dbt.
description: Individual food and drink items that make up our orders, one row per item.

catalog: ANALYTICS_PROD
schema: DBT_AHNINI
table_name: STG_ORDER_ITEMS

dimensions:
  order_item_id:
    sql: '"ORDER_ITEM_ID"'
    format: ID
    #This description was pulled from dbt.
    description: The unique key for each order item.

  order_id:
    sql: '"ORDER_ID"'
    format: ID
    #This description was pulled from dbt.
    description: The corresponding order each order item belongs to

  product_id:
    sql: '"PRODUCT_ID"'
    format: ID

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: stg_order_items
  target_schema: dbt_ahnini
  description: Individual food and drink items that make up our orders, one row per item.
  config:
    materialized: view
  code: |-
    with

    source as (

        -- {# This references seed (CSV) data - try switching to {{ source('ecom', 'raw_items') }} #}
        select * from {{ ref('raw_items') }}

    ),

    renamed as (

        select

            ----------  ids
            id as order_item_id,
            order_id,
            sku as product_id

        from source

    )

    select * from renamed
  referenced_by: [ order_items ]
