# Reference this view as analytics_prod_public__dynamic_connections
catalog: ANALYTICS_PROD
schema: PUBLIC
table_name: DYNAMIC_CONNECTIONS

dimensions:
  test:
    sql: '"TEST"'

measures:
  count:
    aggregate_type: count
