# Reference this view as omni_dbt_raw__raw_customers
schema_label: ""

schema: omni_dbt_raw
table_name: RAW_CUSTOMERS

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  name:
    sql: '"NAME"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: raw_customers
  target_schema: DBT_AHNINI
  config:
    schema: raw
    materialized: seed
  referenced_by: [ stg_customers ]
