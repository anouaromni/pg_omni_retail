# Reference this view as analytics_prod_ani_prod_raw__raw_customers
catalog: ANALYTICS_PROD
schema: ANI_PROD_RAW
table_name: RAW_CUSTOMERS

dimensions:
  id:
    sql: '"ID"'
    format: ID
    primary_key: true

  name:
    sql: '"NAME"'

measures:
  count:
    aggregate_type: count

#The info below was pulled from your dbt repository and is read-only.
dbt:
  name: raw_customers
  target_schema: ANI_PROD
  config:
    schema: raw
    materialized: seed
  referenced_by: [ stg_customers ]
